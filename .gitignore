# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# drizzle — note: drizzle/meta (journal + snapshots) IS tracked on purpose; the
# migrator needs meta/_journal.json to apply migrations from a fresh checkout (CI).

# gitnexus index + generated skills
/.gitnexus
/.claude/skills/generated

# design handoff bundle (reference only, not app code) + temp commit msgs
/Design language for product-handoff/
.commitmsg.tmp
